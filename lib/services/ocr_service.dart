// TODO: camera/image text extraction via google_mlkit_text_recognition
// This service will capture images of paper mind maps, documents, or whiteboards,
// run local OCR text extraction, and return clean strings for parsing.
class OcrService {
  // Future implementation:
  // Future<String> extractTextFromImage(String imagePath) async { ... }
}
