// TODO: voice-to-text via speech_to_text package
// This service will capture user audio, transcribe it locally on-device,
// and feed the raw transcript into the TextParserService.
class VoiceInputService {
  // Future implementation:
  // Future<String> startListening() async { ... }
  // Future<void> stopListening() async { ... }
}
